<!DOCTYPE html>
<html>
<head>
  <title>Donation Cat</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>

  <%= csrf_meta_tags %>


</head>
<body>
<div id="wrap">
<div class="ui blue large top fixed inverted menu">
  <div class="ui container">
    <a class="active item">Home</a>
    <a class="item">Charities</a>
    <a class="item">About</a>

   <div class="right menu">
        <div class="item">
        <div class="ui search">
          <div class="ui icon input">
            <form  action="/search" method="GET">
              <%= tag(:input, :type => "hidden", :name => request_forgery_protection_token.to_s, :value => form_authenticity_token) %>
              <div>
              <input id="search" name="search" type="search" placeholder="Search" required>
              </div>
            </form>
          </div>
        </div>
        </div>
          <% if current_user %>
          <div class="item">
            <a href="/users/<%= current_user.id %>">Welcome, <%= current_user.username %></a>
          </div>
          <div class="item">
          <a class="ui primary button" href="/logout">Log out</a>
          </div>
          <% else %>
          <div class="item">
            <a class="ui primary button" href="/login">Log in</a>
            <%= button_to "New User", new_user_path(@user), :class => "ui primary button", :method => :get  %>
          </div>
          <% end %>
        </div>

  </div>
</div>


<!-- Page Contents -->
<div class="pusher">
  <div class="ui vertical masthead center aligned segment">

    <div class="ui container">




<%= yield %>
</div>
<div class="ui inverted vertical fixed footer segment">
    <div class="ui container">
      <div class="ui stackable inverted divided equal height stackable grid">
        <div class="three wide column">
          <h4 class="ui inverted header">About</h4>
          <div class="ui inverted link list">
            <a href="#" class="item">Sitemap</a>
            <a href="#" class="item">Contact Us</a>
            <a href="#" class="item">Religious Ceremonies</a>
            <a href="#" class="item">Gazebo Plans</a>
          </div>
        </div>
        <div class="three wide column">
          <h4 class="ui inverted header">Services</h4>
          <div class="ui inverted link list">
            <a href="#" class="item">Banana Pre-Order</a>
            <a href="#" class="item">DNA FAQ</a>
            <a href="#" class="item">How To Access</a>
            <a href="#" class="item">Favorite X-Men</a>
          </div>
        </div>
        <div class="seven wide column">
          <h4 class="ui inverted header">Footer Header</h4>
          <p>Extra space for a call to action inside the footer that could help re-engage users.</p>
        </div>
      </div>
    </div>
  </div>
</div>

<%= yield :javascripts %>
</body>
</html>
